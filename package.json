{
  "name": "apryse-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "watch": "tsc -b && vite build --mode development --watch  --minify false --sourcemap inline",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@pdftron/webviewer": "^11.4.0",
    "vite-plugin-static-copy": "^2.3.1"
  }
}
